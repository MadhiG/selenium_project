# Selenium Testing Project

## Overview
A lightweight Python project that uses Selenium and Flask to run UI tests via an HTTP endpoint.

## Setup
```bash
git clone [https://github.com/<YOUR_USERNAME>/selenium_project.git](https://github.com/<YOUR_USERNAME>/selenium_project.git)
cd selenium_project
pip install -r requirements.txt
